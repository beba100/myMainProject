# 📝 Scripts لإضافة المستخدمين

## 📋 الملفات المتوفرة

1. **Add70Users.ps1** - PowerShell Script
2. **Add70Users.py** - Python Script
3. **USERS_LIST.md** - قائمة المستخدمين الـ 70
4. **UsersList.txt** - قائمة بسيطة
5. **UsersList.csv** - قائمة بصيغة CSV

---

## 🚀 كيفية الاستخدام

### الطريقة 1: PowerShell Script

```powershell
cd zaaerIntegration/Scripts
.\Add70Users.ps1
```

**ملاحظة:** قد تحتاج لتثبيت BCrypt.Net-Next package أولاً، أو استخدم Python Script بدلاً منه.

### الطريقة 2: Python Script (موصى به)

```bash
# تأكد من تثبيت المكتبات المطلوبة
pip install pyodbc bcrypt

# تشغيل السكربت
cd zaaerIntegration/Scripts
python Add70Users.py
```

---

## 🔐 معلومات المستخدمين

- **إجمالي المستخدمين:** 70
- **أسماء المستخدمين:** user1 إلى user70
- **كلمة المرور:** `123`
- **التوزيع:** دوري على الفنادق (Tenants) والأدوار (Roles)

---

## 📝 قائمة المستخدمين

راجع الملفات التالية للحصول على قائمة كاملة:
- `USERS_LIST.md` - قائمة مفصلة
- `UsersList.txt` - قائمة بسيطة
- `UsersList.csv` - قائمة بصيغة CSV

---

## ⚠️ ملاحظات مهمة

1. **قبل التشغيل:**
   - تأكد من تشغيل `CreateMasterUsersTables.sql` أولاً
   - تأكد من وجود فنادق (Tenants) في قاعدة البيانات
   - تأكد من وجود أدوار (Roles) في قاعدة البيانات

2. **كلمة المرور:**
   - كلمة المرور الحالية: `123`
   - **يجب تغيير كلمات المرور بعد أول تسجيل دخول**

3. **Connection String:**
   - تم تحديث Connection String في السكربتات
   - Server: s800.public.eu.machineasp.net
   - Database: db32357

---

## 🔧 استكشاف الأخطاء

### خطأ: "BCrypt not available"
- استخدم Python Script بدلاً من PowerShell
- أو قم بتثبيت BCrypt.Net-Next package

### خطأ: "No tenants found"
- تأكد من وجود فنادق في Master DB
- راجع جدول Tenants

### خطأ: "No roles found"
- تأكد من تشغيل CreateMasterUsersTables.sql
- راجع جدول Roles

---

## 📞 الدعم

للمزيد من المعلومات، راجع:
- `AUTHENTICATION_GUIDE.md`
- `Database/CreateMasterUsersTables.sql`

